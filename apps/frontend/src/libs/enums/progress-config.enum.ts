const ProgressConfig = {
	CIRCLE_RADIUS_MULTIPLIER: 2,
	FAKE_LIMIT: 90,
	INTERVAL_MS: 200,
	MAX: 100,
	PERCENT_DIVISOR: 1,
	RESET_DELAY_MS: 100,
	START: 0,
	STEP: 5,
} as const;

export { ProgressConfig };
