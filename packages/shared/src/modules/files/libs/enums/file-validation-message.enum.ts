const FileValidationMessage = {
	CONTENT_TYPE_REQUIRE: "Content-Type is required.",
	CONTENT_TYPE_WRONG: "This host isn't allowed.",
	KEY_REQUIRE: "Key is required.",
	URL_REQUIRE: "URL is required.",
} as const;

export { FileValidationMessage };
