const LandingTestimonialConfig = {
	AUTOPLAY_DELAY_MS: 4000,
	INITIAL_TOUCH_INDEX: 0,
	NEXT_INDEX_INCREMENT: 1,
	PREVIOUS_INDEX_INCREMENT: -1,
	TOUCH_SWIPE_THRESHOLD: 50,
} as const;

export { LandingTestimonialConfig };
