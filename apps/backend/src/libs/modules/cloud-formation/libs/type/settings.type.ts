type Settings = {
	accessKeyId: string;
	botName: string;
	bucketName: string;
	openAIKey: string;
	region: string;
	secretAccessKey: string;
	textGenerationModel: string;
	transcriptionModel: string;
};

export { type Settings };
